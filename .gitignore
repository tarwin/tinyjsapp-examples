/dist/
dist/
.build/
.DS_Store
# ALL payloads live on GitHub Releases (see CLAUDE.md release flow) and were
# purged from git history 2026-07-25; _builds keeps LOCAL staged copies for
# hashing plus the tracked per-app manifest.json files (shipped apps poll
# those raw urls -- never remove them).
_builds/*.dmg
_builds/*/*.zip
_builds/*/*.dmg
_builds/*/*.tar.gz
